# Google Cloud Configuration for Gen AI
# These environment variables are optional if you want to override defaults
# The application will use Application Default Credentials for authentication

# Optional: Override the default project ID
# GOOGLE_CLOUD_PROJECT='your-project-id'

# Optional: Override the default location
# GOOGLE_CLOUD_LOCATION='us-central1'

# Note: Make sure you have authenticated with Google Cloud using:
# gcloud auth application-default login
