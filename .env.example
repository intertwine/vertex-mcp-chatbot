# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=your-gcp-location

# MCP Configuration (optional)
# GitHub MCP Personal Access Token (for example, to access MCP servers on GitHub)
GITHUB_MCP_PAT=your-github-pat-token

# OAuth Test Server (only for development)
OAUTH_CLIENT_ID=mcp-test-client
OAUTH_CLIENT_SECRET=mcp-test-secret